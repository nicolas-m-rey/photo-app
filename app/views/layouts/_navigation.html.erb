<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Photo Manager</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Home', '/', class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Images', '/images', class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Upload', '/images/new', class: "nav-link" %>
        </li>

          <% if !user_signed_in? %>
            <%= link_to "Sign in", new_user_session_path, class: 'btn btn-primary' %>
          <% else %>
             <%= link_to "Sign out", destroy_user_session_path, 'data-turbo-method': :delete, class: 'btn btn-danger logout' %>
          <% end %>
      </ul>
    </div>
  </div>
</nav>